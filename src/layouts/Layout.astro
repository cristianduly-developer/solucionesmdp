---
import '../styles/global.css';
interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}
const {
  title,
  description = "Apps para negocios argentinos. Sin instalación, sin papeles, desde el celular.",
  ogImage = "/og-image.svg",
} = Astro.props;

const siteUrl = "https://www.solucionesmdp.com.ar";
const fullTitle = title === "Inicio" ? "Soluciones MDP — Apps para negocios argentinos" : `${title} | Soluciones MDP`;
---
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{fullTitle}</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Soluciones MDP" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={`${siteUrl}${ogImage}`} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="es_AR" />

    <!-- Twitter / WhatsApp -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={`${siteUrl}${ogImage}`} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  </head>
  <body class="bg-[#0a0a0f] text-slate-100 font-['Inter',sans-serif] antialiased overflow-x-hidden">
    <nav class="fixed top-0 left-0 right-0 z-50 border-b border-white/5 bg-[#0a0a0f]/80 backdrop-blur-md">
      <div class="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
        <a href="/" class="flex items-center">
          <img src="/logo.png" alt="Soluciones MDP" class="h-24 w-auto" />
        </a>
        <div class="hidden md:flex items-stretch h-16">
          <a href="/turnos" class="nav-tab group flex items-center gap-1.5 px-3 text-xs font-medium text-slate-500 border-b-2 border-transparent hover:text-emerald-400 hover:border-emerald-400/60 hover:bg-emerald-400/5 transition-all duration-200">
            <span class="w-1.5 h-1.5 rounded-full bg-emerald-400 opacity-70 group-hover:opacity-100"></span>Turnos
          </a>
          <a href="/presup" class="nav-tab group flex items-center gap-1.5 px-3 text-xs font-medium text-slate-500 border-b-2 border-transparent hover:text-slate-300 hover:border-slate-400/60 hover:bg-slate-400/5 transition-all duration-200">
            <span class="w-1.5 h-1.5 rounded-full bg-slate-400 opacity-70 group-hover:opacity-100"></span>Presup
          </a>
          <a href="/reparto" class="nav-tab group flex items-center gap-1.5 px-3 text-xs font-medium text-slate-500 border-b-2 border-transparent hover:text-blue-400 hover:border-blue-400/60 hover:bg-blue-400/5 transition-all duration-200">
            <span class="w-1.5 h-1.5 rounded-full bg-blue-400 opacity-70 group-hover:opacity-100"></span>Reparto
          </a>
          <a href="/docente" class="nav-tab group flex items-center gap-1.5 px-3 text-xs font-medium text-slate-500 border-b-2 border-transparent hover:text-green-400 hover:border-green-400/60 hover:bg-green-400/5 transition-all duration-200">
            <span class="w-1.5 h-1.5 rounded-full bg-green-400 opacity-70 group-hover:opacity-100"></span>Docente
          </a>
          <a href="/eventos" class="nav-tab group flex items-center gap-1.5 px-3 text-xs font-medium text-slate-500 border-b-2 border-transparent hover:text-amber-400 hover:border-amber-400/60 hover:bg-amber-400/5 transition-all duration-200">
            <span class="w-1.5 h-1.5 rounded-full bg-amber-400 opacity-70 group-hover:opacity-100"></span>Eventos
          </a>
          <a href="/gastronomia" class="nav-tab group flex items-center gap-1.5 px-3 text-xs font-medium text-slate-500 border-b-2 border-transparent hover:text-orange-400 hover:border-orange-400/60 hover:bg-orange-400/5 transition-all duration-200">
            <span class="w-1.5 h-1.5 rounded-full bg-orange-400 opacity-70 group-hover:opacity-100"></span>Gastro
          </a>
          <a href="/membresias" class="nav-tab group flex items-center gap-1.5 px-3 text-xs font-medium text-slate-500 border-b-2 border-transparent hover:text-teal-400 hover:border-teal-400/60 hover:bg-teal-400/5 transition-all duration-200">
            <span class="w-1.5 h-1.5 rounded-full bg-teal-400 opacity-70 group-hover:opacity-100"></span>Membresías
          </a>
          <a href="/web" class="nav-tab group flex items-center gap-1.5 px-3 text-xs font-medium text-slate-500 border-b-2 border-transparent hover:text-violet-400 hover:border-violet-400/60 hover:bg-violet-400/5 transition-all duration-200">
            <span class="w-1.5 h-1.5 rounded-full bg-violet-400 opacity-70 group-hover:opacity-100"></span>Web
          </a>
        </div>
        <a href="https://wa.me/542235767784" target="_blank"
          class="text-sm font-medium bg-indigo-600 hover:bg-indigo-500 text-white px-4 py-2 rounded-lg transition-colors">
          Consultar
        </a>
      </div>
    </nav>
    <!-- Scroll beam — solo desktop -->
    <div class="fixed left-6 xl:left-10 top-0 bottom-0 pointer-events-none z-30 hidden lg:block" aria-hidden="true">
      <!-- Track faint -->
      <div class="absolute left-1/2 -translate-x-1/2 top-16 bottom-0 w-px bg-white/[0.04]"></div>
      <!-- Línea rellena -->
      <div id="beam-fill"
        class="absolute left-1/2 -translate-x-1/2 top-16 w-px origin-top"
        style="height: calc(100% - 4rem); transform-origin: top; transform: scaleY(0) translateX(-50%); background: linear-gradient(to bottom, transparent, #818cf8 40%, #a5b4fc);">
      </div>
      <!-- Spark en la punta -->
      <div id="beam-spark"
        class="absolute left-1/2 -translate-x-1/2 w-2 h-2 rounded-full"
        style="top: 4rem; background: #c7d2fe; box-shadow: 0 0 6px 3px rgba(165,180,252,0.9), 0 0 20px 6px rgba(129,140,248,0.5); opacity: 0;">
      </div>
      <!-- Halo suave que sigue al spark -->
      <div id="beam-halo"
        class="absolute left-1/2 -translate-x-1/2 w-8 h-8 rounded-full -translate-y-1/2"
        style="top: 4rem; background: radial-gradient(circle, rgba(129,140,248,0.15) 0%, transparent 70%); opacity: 0;">
      </div>
    </div>

    <main class="pt-16">
      <slot />
    </main>
    <footer class="border-t border-white/5 mt-24 py-12">
      <div class="max-w-6xl mx-auto px-4 flex flex-col md:flex-row items-center justify-between gap-4 text-sm text-slate-500">
        <div class="flex items-center gap-3">
          <img src="/logo.png" alt="Soluciones MDP" class="h-24 w-auto" />
          <p>© 2025 Soluciones MDP. Mar del Plata, Argentina.</p>
        </div>
        <a href="https://wa.me/542235767784" target="_blank" class="hover:text-white transition-colors">
          Contacto por WhatsApp
        </a>
      </div>
    </footer>
  </body>
</html>

<script>
  const fill = document.getElementById('beam-fill');
  const spark = document.getElementById('beam-spark');
  const halo = document.getElementById('beam-halo');
  if (fill && spark && halo) {
    const OFFSET = 64; // altura del nav
    let ticking = false;

    function update() {
      const scrolled = window.scrollY;
      const total = document.documentElement.scrollHeight - window.innerHeight;
      const progress = total > 0 ? Math.min(scrolled / total, 1) : 0;

      // La línea crece desde 0 hasta el 100% del track
      fill.style.transform = `scaleY(${progress}) translateX(-50%)`;

      // El spark y el halo se mueven al frente de la línea
      const trackHeight = window.innerHeight - OFFSET;
      const y = OFFSET + progress * trackHeight;
      spark.style.top = `${y}px`;
      halo.style.top = `${y}px`;

      // Fade in al empezar a scrollear, fade out al llegar al final
      const opacity = progress > 0.01 && progress < 0.99 ? 1 : 0;
      spark.style.opacity = String(opacity);
      halo.style.opacity = String(opacity * 0.8);

      // Pequeño pulso: escala el spark ligeramente con el scroll
      const pulse = 1 + Math.sin(scrolled * 0.05) * 0.3;
      spark.style.transform = `translateX(-50%) scale(${pulse})`;

      ticking = false;
    }

    window.addEventListener('scroll', () => {
      if (!ticking) {
        requestAnimationFrame(update);
        ticking = true;
      }
    }, { passive: true });

    // Estado inicial
    update();
  }
</script>
